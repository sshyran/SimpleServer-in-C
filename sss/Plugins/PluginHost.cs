using System;
using System.Collections.Generic;
using System.Text;

namespace SimpleServer.Plugins
{
    public class PluginHost
    {
        bool guiAttached = false;
        internal void Initialize(int guiPort = -1)
        {
            if (guiPort != -1)
            {

            }
        }
        internal void AttachGui(int port)
        {

        }
    }
}
